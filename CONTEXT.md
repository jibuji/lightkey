用平实自然的简体中文说清楚，不要堆砌抽象类名词。

# LightKey

轻钥（LightKey）：个人密钥/私密信息管理工具（lk-core / lk-daemon / lk-cli / 桌面壳），
客户端全开源（MIT）。本文件是唯一上下文的统一词汇表，只收语言不收设计细节；
设计决策见 `docs/decisions.md` 与 `docs/README.md`。

## Language

### 条目域

**条目集合**:
一次解锁会话内加载进前端的全量条目快照，是列表类型筛选与搜索的唯一输入。
_Avoid_: 条目缓存, items 数组

**条目草稿**:
表单产出、尚无 id 与 revision 的条目载荷（`ItemDraft`）；新建直接入库，
编辑经乐观并发（CAS）提交。
_Avoid_: 表单数据, item 载荷

**可搜字段白名单**:
允许进入搜索 haystack 的显式字段集合——名称 / 用户名 / 域名(login uris) /
用途(secret purpose) / 文件备注与元数据；密钥明文值与笔记全文绝不入列
（spec §6.2 安全决策）。
_Avoid_: 全文索引, 全文搜索

### 数据对象

**对象**：
条目与规则的统称——共同的形态特征：K_data 密封密文、经同一加密索引发现变更与同步、同走软删/墓碑/30 天硬删。规则引入后索引与同步从「条目级」扩为「对象级」。
_Avoid_: 条目（「对象」是上位词，勿窄化）

**条目**：
用户数据对象，分四类——login（账号+密码+网址+自定义字段）/ note（Markdown 笔记）/ secret（密钥值+用途）/ file（加密附件 ≤50MB）。「对象」的一种。
_Avoid_: 记录、档案（勿与「条目集合」前端快照混）

**规则**：
授权配置对象——绑定项目目录（inject 时还绑定具名命令，可 glob）与授权的 key 名集合，声明能力（inject/read；语义见授权门小节）。「对象」的一种，与条目同索引同步、同墓碑路径。
_Avoid_: 授权规则、白名单规则（「规则」即指该对象）

**条目类型**：
条目的四类 schema——login / note / secret / file，决定字段集与能力边界：**仅 secret 可注入**（login/note/file 请求按第 1 层拒绝，与「不存在」不区分）；读规则按条目名匹配、不区分类型。
_Avoid_: 条目种类、item type

**墓碑**：
对象软删除的标记密文（30 天延迟硬删）；跨端同步据此收敛删除，防「删了又回来」。
_Avoid_: 删除标记、软删记录

### 守护进程宿主（C 层）

**命令锁**：
守护进程中串行化 RPC 命令处理的互斥锁；持有期间其他命令排队。
_Avoid_: daemon 锁、全局锁

**执行计划路由**：
daemon 把每个 RPC 方法映射到一种执行策略的唯一分发点；方法的锁纪律由其策略声明，不在各处理函数里手写。
_Avoid_: 分发表、dispatch 拦截

**两阶段同步**：
同步轮次的并发结构——抓取阶段只持短读锁并完成全部网络 I/O，应用阶段在短写锁内落盘；网络 I/O 全程不持命令锁。
_Avoid_: 无锁同步、后台同步

### 协议层

**协议契约**：
线协议（RPC 方法名 / 通知帧名 / 字符串错误码 / 审计通道值）的单一事实来源——Rust 权威源 `crates/lk-core/src/ipc.rs`，TS 镜像 `frontend/src/ipc/protocol.ts`，双向对齐由 `frontend/src/__tests__/protocolContract.test.ts` 钉死（漂移在 CI 失败，不在用户端错配）；适配器/桥只取常量，不手写字面量。
_Avoid_: 协议（「协议契约」强调跨语言单一来源 + 对齐测试，防 #85/#86 类漂移）

### 授权门

**授权门**：
约束 agent/进程「注入与值披露」两类动作的三层裁决机制——默认拒绝 → 规则白名单 → 弹窗审批；任何放行都可审计、可回溯。桌面内嵌直调受信豁免（不进门）；元数据（`item.list` / `rule.list`）只过令牌门，不进门。
_Avoid_: 权限系统、访问控制

**受信豁免**：
桌面内嵌直调（`channel=desktop`）跳过授权门的规则/弹窗层直接放行值披露；豁免的是**裁决层**，不是认证——桌面直调仍校验会话令牌。**仅值披露通道（`item.get` / `item.export`）有豁免**；注入通道（`authz.evaluate`）无豁免，桌面来源同样走完整三层。socket/CLI 通道一律走门，无豁免。
_Avoid_: 免审批、白名单通道

**认证**：
请求方向守护进程证明「你是谁」——持有效会话令牌即通过。只确认身份，不等于有权拿值。
_Avoid_: 鉴权（常与「授权」混为一谈）

**授权**：
决定「你能拿什么值」的裁决——**值的披露是授权事件**，须经规则命中或弹窗批准；令牌只做认证，不做授权（桌面内嵌直调受信豁免除外）。
_Avoid_: 权限、放行（在区分认证/授权语境时）

**令牌**：
一次解锁窗口内的认证凭据：解锁时签发、锁定即删、每次解锁轮换。持令牌进程可见条目/规则元数据（`item.list` / `rule.list`），但不产生任何读值能力。
_Avoid_: 会话、session、API key

**三层授权门**：
授权门的三个裁决层——① 默认拒绝（启动者未知 / cwd 不可得 / 规则库损坏 / 请求 key 无法解析 / 无审批界面 → fail-closed）② 规则白名单（按项目目录自动放行）③ 弹窗审批（人类明确允许，30s 超时默认拒绝）。每层结果都进审计。
_Avoid_: 授权流程

**规则白名单**：
vault 内加密、按项目目录绑定的规则；命中即自动放行该规则授权的 key 名集合。
_Avoid_: 白名单、allowlist（脱离「按目录绑定 + 加密」语境时）

**规则管理审批门**：
`rule.add` / `rule.remove` 本身是授权事件（对称原则：授权的建立与撤销都要
桌面审批，防 agent 自我提权 / 拆墙），socket/pipe 通道走弹窗审批、headless
fail-closed、桌面直调豁免（补充拍板 #22）。与「受信豁免」之别：豁免的是
值披露的**裁决层**，规则门豁免的是 desktop 直调本身。
_Avoid_: 规则审批、规则变更审批

**弹窗审批**：
授权门第 3 层——桌面弹窗由人类明确 Allow/Deny，30s 超时默认拒绝。指**裁决层**；与「审批延迟执行」（执行策略）是不同概念。
_Avoid_: 审批（单独用时易与执行策略混淆）

**值披露**：
把条目的值（secret 明文 / 附件原始数据）交给请求方本身的动作；是一个授权事件——令牌本身不授权披露。与「注入」相对：注入把值送进子进程环境、不交给请求方。
_Avoid_: 数据导出、值访问

**注入**：
把条目的值作为环境变量送进被批准命令的子进程（`lk inject`）；请求方（agent）只收到「已注入/已拒绝」，看不到值——lk 自身绝不向 stdout/日志/审计输出值。与「值披露」相对，二者都是授权事件、都过授权门。
_Avoid_: 值披露（二者是不同的值流动）

**规则能力**：
规则授权的行为类型：inject（注入）| read（读值）；**能力不互授**——inject 规则不授权读，read 规则不授权注入。
_Avoid_: 权限类型

**读规则**：
能力为 read 的规则：无命令绑定，按条目名精确匹配，授权 socket/CLI 通道按名读条目值（`item.get`）；不豁免 `item.export`。
_Avoid_: 读权限、读白名单

**临时解锁**：
锁定态下的一次性解锁材料，只服务「本次授权/披露」这一件事——不签发令牌、不写 session.token、不置 shared.vault，授权结束即销毁，vault 保持锁定。**注入（#67）与读通道（#23）都用**；headless / 未初始化库锁态维持 fail-closed 拒绝。
_Avoid_: 临时会话、临时令牌

**一体化解锁**：
锁定态 + 桌面 UI 在场时把「临时身份确认（主密码）+ 本次授权」折叠成桌面弹窗的一次交互——注入 `lk inject`（#67）、值披露 `item.get` / `item.export`（补充拍板 #23）。协议零新增：`needsUnlock` / `masterPassword` / `kind` 三要素承载；临时 vault 单次即毁，不产生 item.* 全量能力（#65）。
_Avoid_: 免密解锁、自动解锁

**启动者**：
对端请求可归属的顶层进程（终端 / 编辑器 / agent），经进程链回溯判定；**对端进程自身不可读 → unknown → fail-closed 拒绝**；中间祖先缺失 → 取已回溯的最顶层（仍真实）。
_Avoid_: 调用者、请求方

**审批延迟执行**：
授权门 `authz.evaluate` 的**执行策略**——命令锁内预检与登记，锁外等待审批决策（≤配置审批超时，默认 30s，超时默认拒绝），重取命令锁收尾。与「弹窗审批」裁决层是不同概念。
_Avoid_: 异步审批、长事务

### 凭据

**主密码**：
用户记忆的唯一身份凭据，经 Argon2id 派生主密钥；安全策略留 Rust——≥8 位（建库/恢复统一校验）、解锁/恢复经 AuthGuard 限流防暴破（失败计数 + 指数退避）、错误密码统一「解锁失败」文案防探测。
_Avoid_: 密码、口令（「密码」还指条目字段与同步凭据）

**恢复码**：
高熵 40 字符（39 随机 + 1 校验位）备份凭证；仅展示一次，应用不记忆、不重展示。配合**新设主密码**解信封取回主密钥完成恢复；无恢复码只能重置为新库（旧数据不可解密）。
_Avoid_: 恢复密钥、备份密码

**恢复信封**：
用恢复码独立派生的 K_recovery 加密的主密钥副本；可随库进 BYO 云而不破坏零知识。恢复完成后以新恢复码重新生成、旧信封作废。
_Avoid_: 备份文件、密钥备份

**主密钥**：
从主密码派生的根密钥（32B），HKDF 分叉出两把功能密钥——K_data（数据加密）与 K_audit（审计 HMAC）；恢复信封封存的正是它。
_Avoid_: 根密钥（避免与「功能密钥」混淆）

**三通道模型**：
用户可凭三样东西恢复数据——主密码、恢复码、已信任设备；**全丢则数据在数学上不可恢复**，产品文案如实声明、不暗示可找回。
_Avoid_: 找回、客服恢复

### 跨子系统

**桥**：
Windows 侧 `lk.exe bridge` 的 stdio 中继——Linux `lk` 把 JSON-RPC 帧经 interop 管道交给它，中继到 Windows 命名管道上的守护实例，响应原样回传。零网络依赖、零新增监听面、启动者取证保留。
_Avoid_: 代理、中继服务器（「桥」即产品名 `lk bridge`）

**连接目标**：
Linux `lk` 按运行环境自动判定的连通去向——原生 Linux 连本地 UDS 守护实例；WSL2 连 Windows 主机 GUI（经桥）。探测分型：装了连不上明确报错、没装静默本地，**绝不静默回落防空库错觉**；目标对用户可见。
_Avoid_: 后端、对端（「连接目标」强调可见的判定结果）

**WSL 命名空间**：
同一项目目录在 Windows（UNC）与 Linux 各有一种写法；跨子系统时两侧同过同一归一化函数折算为 `wsl://<distro>/<rest>` 规范形再做规则祖先匹配——防伪造 cwd 变体绕过或漏配。经桥的审计记 `channel=wsl-bridge`。
_Avoid_: WSL 路径、UNC 路径（规范形以 `wsl://` 为锚）
