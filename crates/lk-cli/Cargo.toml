[package]
name = "lk-cli"
description = "LightKey 命令行工具（`lk`）：条目管理、解锁/锁定、同步、规则、inject、守护进程"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "lk"
path = "src/main.rs"

[dependencies]
lk-core = { path = "../lk-core" }
clap.workspace = true

# 骨架占位：命令树已声明，行为在 M0 里程碑实现。
# 后续里程碑按需补充：clap_complete、anyhow、tracing、serde_json 等。
