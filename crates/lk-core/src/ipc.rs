//! 本地 IPC 协议（规格：`docs/ipc.md`）。
//!
//! 设计要点（均为决议拍板，勿自行变更）：
//!
//! - 传输：Unix domain socket / Windows named pipe；协议：JSON-RPC 2.0。
//! - 会话令牌随解锁轮换；`lk` 常驻守护进程持解锁态，密钥只存在于守护进程内存。
//! - CLI / 桌面 / 浏览器扩展（Native Messaging，M3）统一走守护进程。
//! - 响应只含已解密最小字段；锁屏/超时自动锁定。
//!
//! 占位模块：M0（守护进程解锁态）起在此实现。
